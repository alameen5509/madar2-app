[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["cd Mdar.API && dotnet restore"]

[phases.build]
cmds = ["cd Mdar.API && dotnet publish -c Release -o /app/publish"]

[start]
cmd = "cd /app/publish && dotnet Mdar.API.dll --urls http://0.0.0.0:$PORT"
